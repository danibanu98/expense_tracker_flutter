# --- Început Fișier Nou ---
platform :ios, '13.0'

ENV['LANG'] = 'en_US.UTF-8'

target 'Runner' do
  use_frameworks!
  use_modular_headers!

  flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))
end
# --- Sfârșit Fișier Nou ---